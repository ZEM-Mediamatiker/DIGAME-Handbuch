<section class="search-row">

    {% set banner = page.media.images|first %}

    <!--{% if banner %}
    <div class="search-image">
        <a href="{{ page.url }}"><img src="{{ banner.cropZoom(100,100) }}" /></a>
    </div>
    {% endif %}-->
    <style>
		#navigation{
		display: none;
		}
	</style>
    <div class="search-item">
    	<div class="search-result-header">
        	<div class="search-title">
            	<h3 class="searchresulttitle"><a href="{{ page.url }}">{{ page.title }}</a></h3>
        	</div>

        	<div class="search-details">
            	<span class="search-date">Geändert: {{ page.date|date(config.system.pages.dateformat.short) }}</span>
        	</div>
        </div>
		
		<div class="search-result">
        	<p>{{ page.summary }}</p>
        </div>

        
    </div>
</section>